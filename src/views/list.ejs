<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Bem-vindo | To-Do App</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="./assets/style.css">
  </head>
<body class="vh-100 d-flex flex-column">
    
    <%- include('partials/header') %>


    <ul class="lists border rounded p-0">

      <% if (tasksToDo === 0 || tasksCompleted === 0) {%>
        <li class="d-flex justify-content-center align-items-center list-group-item border rounded p-3 m-3">
          <span class="text-muted">Não existe nenhuma tarefa!</span>
        </li>
      <% }  else { %>

        
      <% tasksToDo.forEach(task => { %>
      <li class="d-flex justify-content-between align-items-center list-group-item border rounded p-3 m-3">
        <a class="btn fs-5" href="/app/<%= task.id %>"> <%= task.name %> </a>
        
      </li>
      <% }); %>

      <% } %>
    </ul>


    <%- include('partials/footer') %>

</body>
</html>